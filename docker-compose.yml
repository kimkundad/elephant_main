services:
  nginx:
    image: nginx:alpine
    container_name: elephant-nginx
    ports:
      - "8083:80"
    volumes:
      - ./:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php

  php:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: elephant-php
    volumes:
      - ./:/var/www
    environment:
      TZ: Asia/Bangkok
